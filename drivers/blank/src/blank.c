#include <mos/sys.h>
#include <mos/drv.h>
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

GENERATE_METADATA(metadata) = {
    .friendlyName = "blank",
};

void _mdrvmain()
{
    // use this as a template for other drivers

    while (1)
        ;
}